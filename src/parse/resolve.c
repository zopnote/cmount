#include "parse.h"

#include <stddef.h>
#include <stdio.h>


void parse_resolveString(
    const char* string,
    const ParseFormat format,
    ParseEntry entriesBuffer[],
    size_t entriesBufferSize
    ) {
    printf("%s", string);
}